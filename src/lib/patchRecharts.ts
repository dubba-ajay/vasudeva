// Previously attempted to delete Recharts.defaultProps to silence warnings.
// That approach breaks Recharts runtime which relies on defaultProps. Leave this file as a no-op to avoid runtime failures.

// Intentionally empty. Do NOT modify Recharts components at runtime.

export default {};
